language: node_js

node_js:
    - 12

services:
  - docker

before_install:
  #install heroku CLI
  - wget -qO- https://toolbelt.heroku.com/install.sh | sh

script:
  - docker build -t registry.heroku.com/palnet/web .

deploy:
  provider: script
  script:
    bash docker_push
  on:
    branch: scotty_pal

# safelist
branches:
  only:
  - master
  - scotty_aaa
  - scotty_actnearn
  - scotty_bloque64
  - scotty_pal
  - scotty_spt
  - scotty_steemcoinpan
  - scotty_weedcash
